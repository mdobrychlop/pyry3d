#ifndef LOGGER_PYRY3D 
#define LOGGER_PYRY3D 

#include <string>
#include <fstream>

class Logger {

    private:
        std::ofstream logfile;
    public:
        Logger();
        void set_log(std::string filename);

        void write_to_logfile(int complex_index);
};

#endif